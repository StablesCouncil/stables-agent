UI: refine index end-CTA and footer spacing for readability

Make the index "Want to know more?" block match the final CTA style and increase compact footer spacing so the "The Playing Field" action has clearer breathing room.
