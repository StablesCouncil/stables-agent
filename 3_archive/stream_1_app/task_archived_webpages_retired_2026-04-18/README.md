# Webpages (retired)

**Authoring moved to `../github_pages_root/`**, which uses the **same directory layout** as the GitHub Pages repo root (see **`../github_pages_root/CANONICAL_LAYOUT.md`**).

The old **`pages/`** tree was moved to **`3_archive/stream_1_app/task_archived_webpages_pages_2026-04-16/pages/`** for history.

**Sync** no longer reads this folder. Use **`npm run sync:site`** from the task folder root only.
