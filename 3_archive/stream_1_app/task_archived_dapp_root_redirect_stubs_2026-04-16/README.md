# Archived: root `/dapp/latest-version/` and `/dapp/previous-versions/` redirect stubs

**Date:** 2026-04-16  
**Reason:** Retired from the active dev tree. Showcase install paths should use **`dapp/1-showcase/latest-version/`** only. Retired **previous-versions** documentation lives under **`3_archive/stream_1_app/task_archived_dapp_channel_previous_versions_2026-04-16/dapp/`** (active **`dapp/`** no longer ships **`previous-versions/`**).

Each subfolder here contained a single **`index.html`** that redirected browsers to the matching path under **`dapp/1-showcase/`**.

**Note:** After the next push to GitHub Pages, old URLs that pointed at **`.../dapp/latest-version/`** or **`.../dapp/previous-versions/`** at the repo root will **404** unless Council restores redirects elsewhere (e.g. Pages or a reverse proxy).
