# Archived `webpages/pages` (pre `github_pages_root`)

**Date:** 2026-04-16 (calendar date on folder; archive created during unification).

This is the former **`task_stablescouncil_github_io/webpages/pages/`** tree: slug-per-folder sources that **`sync-site.mjs`** used to merge with **`static/`** into **`site/`**.

**Replacement:** edit **`1_development/stream_1_app/task_stablescouncil_github_io/github_pages_root/`** only (same paths as the live site root, except **`dapp/`** overlaid from **`stream_1_app/dapp/`**).

Use this archive only for **history or file recovery**.
