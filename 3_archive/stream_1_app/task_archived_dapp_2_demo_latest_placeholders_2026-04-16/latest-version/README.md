# Demo channel — `latest-version`

When Council publishes a **demo** line **`.mds.zip`** (see **`demo/build/README.md`**), place the current package here.

**Policy:** `0_handshake/minidapp_version.md`.
